{% extends "AppBundle::table.html.twig" %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="#">Home</a> 
          <i class="icon-angle-right"></i>         
        </li> 
        <li>
      <a href="{{ path('session_index',{ 'id': session.concours.id }) }}">les sessions de {{session.concours.nom}}</a> 
          <i class="icon-angle-right"></i>          
        </li>                
        <li><a href="#">Edition</a></li>
      </ul>
   {% endblock %}
 {% block table_title %}Edition d'une session de {{session.concours.nom}} ~{{session.nom}}{% endblock %}  
{% block table %}

  {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Enrégistrer" class="btn btn-primary "/>
    {{ form_end(edit_form) }}
    <ul>
        <li>
            <a href="{{ path('session_index', { 'id': session.concours.id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
