{% extends "AppBundle::table.html.twig" %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="#">Home</a> 
          <i class="icon-angle-right"></i>         
        </li> 
        <li>
      <a href="{{ path('session_index',{ 'id': concour.id }) }}">les Sessions de {{concour.nom}}</a> 
          <i class="icon-angle-right"></i>          
        </li>              
        <li><a href="#">Création</a></li>
      </ul>
   {% endblock %}
 {% block table_title %}Création d'une session de {{concour.nom}}{% endblock %}  
{% block table %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enrégistrer" class="btn btn-primary "/>
    {{ form_end(form) }}
    <ul>
        <li>
            <a href="{{ path('session_index',{ 'id': concour.id }) }}" >Back to the list</a>
        </li>
    </ul>
{% endblock %}
