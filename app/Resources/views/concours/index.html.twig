{% extends "AppBundle::table.html.twig" %}
{% block page_title %} Ecoles{% endblock %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
         <a href="#">Home</a> 
          <i class="icon-angle-right"></i>
        </li>
        <li><a href="#">Ecoles</a></li>
      </ul>
   {% endblock %}
    {% block table_title %}Ecoles{% endblock %}  
   {% block table %}
         <table class="table table-striped table-bordered bootstrap-datatable datatable" id="my-table-1">
        <thead>
            <tr>
                <th class="col-md-10">Ecoles</th>
                <th class="col-md-2">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for concour in concours %}
               <tr>
                <td><a href="{{ path('session_index', { 'id': concour.id }) }}">
                  <h2>{{ concour.nom }}</h2>
                  <p>{{ concour.ecole }}</p>
                </a>
                </td>
                <td>
           <a href="{{ path('concours_edit', { 'id': concour.id }) }}">Modifier</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('concours_new') }}" class="btn btn-default ">Creer une école</a>  
{% endblock %}
