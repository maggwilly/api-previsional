{% extends "AppBundle::layout.html.twig" %}
{% block title %}Question creation{% endblock %}
{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('question_index', { 'id': partie.id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
