{% extends "AppBundle::table.html.twig" %}
{% block page_title %}Nouveau lien{% endblock %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="{{ path('homepage')}}">Home</a> 
          <i class="icon-angle-right"></i>         
        </li>
        <li>
      <a href="{{ path('session_show', { 'id': session.id }) }}">{{session.nomConcours}} ~ {{session.nom}} </a> 
          <i class="icon-angle-right"></i>          
        </li>  
        <li>        
        <li> 
      <a href="{{ path('objectif_index', { 'id': session.id  }) }}">Liens utils</a> 
          <i class="icon-angle-right"></i>          
        </li>  
        <li>
      <a href="#">Nouveau lien</a>           
        </li>               
      </ul>
   {% endblock %}
 {% block table_title %}Nouveau lien pour  ~ {{session.nomConcours}}{% endblock %}  
{% block table %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enrégistrer" class="btn btn-primary"/>
    {{ form_end(form) }}

 <a href="{{ path('objectif_index', { 'id': session.id }) }}">Retour</a>

{% endblock %}
