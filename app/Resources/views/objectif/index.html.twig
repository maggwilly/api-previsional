{% extends "AppBundle::table.html.twig" %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="#">Home</a> 
          <i class="icon-angle-right"></i>         
        </li>
        <li>
      <a href="{{ path('session_show', { 'id': session.id }) }}">{{session.nomConcours}} ~ {{session.nom}} </a> 
          <i class="icon-angle-right"></i>          
        </li>  
        <li>
      <a href="#">Les liens utils de</a>    
        </li>               
      </ul>
   {% endblock %}
 {% block table_title %}Les liens utils de {{session.nomConcours}} ~ {{session.nom}} {% endblock %}  
{% block table %}
    <table class="table table-striped table-bordered" id="my-table-1">
        <tbody>
        {% for objectif in liens %}
            <tr>
                <td>{{ objectif.titre }}</td>
                <td><a href="{{ objectif.description }}"> Afficher</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('objectif_show', { 'id': objectif.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('objectif_edit', { 'id': objectif.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>                
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <ul>
        <li>
            <a href="{{ path('objectif_new', { 'id': session.id }) }}">Nouveau lien util</a>
        </li>
    </ul>
{% endblock %}
