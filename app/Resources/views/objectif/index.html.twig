{% extends "AppBundle::table.html.twig" %}
{% block page_title %}Les liens utils{% endblock %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="{{ path('homepage')}}">Home</a> 
          <i class="icon-angle-right"></i>         
        </li>
        <li>
      <a href="{{ path('session_show', { 'id': session.id }) }}">{{session.nomConcours}} ~ {{session.nom}} </a> 
          <i class="icon-angle-right"></i>          
        </li>  
        <li>
      <a href="#">Les liens utils</a>    
        </li>               
      </ul>
   {% endblock %}
 {% block table_title %}Les liens utils de {{session.nomConcours}} ~ {{session.nom}} {% endblock %}  
{% block table %}
    <table class="table table-striped table-bordered" id="my-table-1">
        <tbody>
        {% for objectif in liens %}
            <tr>
                <td>{{ objectif.titre }}</td>
                <td><a href="{{ objectif.description }}"> Afficher la cible</a></td>
                <td>
   <a href="{{ path('objectif_edit', { 'id': objectif.id }) }}">Modifier</a>
                </td>                
            </tr>
        {% endfor %}
        </tbody>
    </table>
   <a href="{{ path('objectif_new', { 'id': session.id })  }}" class="btn btn-default">Nouveau lien util</a>

{% endblock %}
