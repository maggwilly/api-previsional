{% extends "AppBundle::table.html.twig" %}
{% block page_title %}Les programmes de prepa{% endblock %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="{{ path('homepage')}}">Home</a> 
          <i class="icon-angle-right"></i>
        </li>
        <li><a href="#">Les programmes de prepa</a></li>
      </ul>
   {% endblock %}
   {% block table_title %}Les programmes de prepa{% endblock %}  
{% block table %}
        <table class="table table-striped table-bordered bootstrap-datatable datatable" id="my-table-1">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for concour in concours %}
            <tr>
                <td>
                  <a href="{{ path('matiere_index', { 'id': concour.id }) }}">
                     <h4> {{ concour.nom }}</h4>
                    <p>{{ concour.abreviation }}</p> 
                    </a>

                </td>
            <a href="{{ path('programme_edit', { 'id': concour.id }) }}">Modifier</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
 <a href="{{ path('programme_new') }}" class="btn btn-default ">Creer un programme de prépa</a> 
{% endblock %}
