{% extends "AppBundle::table.html.twig" %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="#">Home</a> 
          <i class="icon-angle-right"></i>         
        </li>
        <li>
      <a href="{{ path('session_show', { 'id': session.id }) }}">{{session.nomConcours}} ~ {{session.nom}} </a> 
          <i class="icon-angle-right"></i>          
        </li>  
        <li>        
        <li> 
      <a href="{{ path('objectif_index', { 'id': session.id  }) }}">Document</a> 
          <i class="icon-angle-right"></i>          
        </li>  
        <li>
      <a href="#">Création</a>           
        </li>               
      </ul>
   {% endblock %}
 {% block table_title %}Création d'un document dans {{session.nomConcours}}{% endblock %}  
{% block table %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enrégistrer" />
    {{ form_end(form) }}
    <ul>
        <li>
            <a href="{{ path('ressource_index', { 'session': session.id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
