{% extends "AppBundle::layout.html.twig" %}
{% block title %}Partie edit{% endblock %}
{% block body %}
<div class="card"> 
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}
</div>
    <ul>
        <li>
            <a href="{{ path('partie_index', { 'id': partie.matiere.id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
