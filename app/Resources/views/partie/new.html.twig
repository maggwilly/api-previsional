{% extends "AppBundle::table.html.twig" %}
{% block page_title %}Nouvelle unité{% endblock %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="{{ path('homepage')}}">Home</a> 
          <i class="icon-angle-right"></i>
        </li> 
          <li>
          <a href="{{ path('matiere_index', { 'id': matiere.programme.id }) }}">{{matiere.programme.nom}}</a> 
          <i class="icon-angle-right"></i>
        </li>   
          <li>
          <a href="{{ path('partie_index', { 'id': matiere.id }) }}">{{matiere.titre}}</a> 
          <i class="icon-angle-right"></i>
        </li>                
      </ul>
   {% endblock %}
   {% block table_title %}Nouvelle unité de préparation{% endblock %} 
{% block table %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enrégistrer" class="btn btn-info"/>
    {{ form_end(form) }}
    <ul>
         {% if is_granted('ROLE_SUPERVISEUR') %}
        <li>
             <a href="{{ path('partie_index', { 'id': matiere.id }) }}">Retour</a>
        </li>
        {% else %}
         <li>
            <a href="{{ path('partie_index') }}">Retour</a>
        </li>
        {% endif %}
    </ul>
{% endblock %}
 {% block javascripts %}
{{parent()}}
   <script>
  CKEDITOR.replace( 'form_objectif' );
  </script>
 {% endblock %}   