{% extends "AppBundle::layout.html.twig" %}
{% block title %}Matiere editt{% endblock %}
{% block body %}
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('concours_show', { 'id': matiere.programme.id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
