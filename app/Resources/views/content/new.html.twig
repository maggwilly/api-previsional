{% extends "AppBundle::layout.html.twig" %}
{% block title %}Content creation{% endblock %}
{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('content_index', { 'id': article.id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
