{% extends "AppBundle::table.html.twig" %}
{% block page_title %}Nouvelle publication{% endblock %}
      {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="{{ path('homepage')}}">Home</a> 
          <i class="icon-angle-right"></i>
        </li>
        <li>
            <a href="{{ path('notification_index') }}">Communication</a>
           <i class="icon-angle-right"></i>
        </li>
        <li>
            <a href="#">Nouvelle publication</a>
        </li>
      </ul>
   {% endblock %}
   {% block table_title %}Nouvelle publication{% endblock %} 
{% block table %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enregistrer" class="btn btn-info"/>
    {{ form_end(form) }}

<a href="{{ path('notification_index') }}"><i class="halflings-icon chevron-left"></i> Retour</a>
{% endblock %}
 {% block javascripts %}
{{parent()}}
   <script>
   $( 'textarea.ckeditor' ).ckeditor(function( textarea ) {
         $( 'textarea.ckeditor' ).val( 'My new content' );
 });
  //initSample();
  </script>
 {% endblock %}    