{% extends "AppBundle::table.html.twig" %}
{% block page_title %}{{price.nom}}{% endblock %}
  {% block page_sub_title %}
       <ul class="breadcrumb">
        <li>
          <i class="icon-home"></i>
          <a href="{{ path('homepage')}}">Home</a> 
          <i class="icon-angle-right"></i>
        </li>
        <li><a href="{{ path('price_index') }}">Grille des tarifs</a>
            <i class="icon-angle-right"></i>
        </li>
        <li><a href="#">{{price.nom}}</a>   
        </li>        
      </ul>
   {% endblock %}
   {% block table_title %}Détails{% endblock %}
    {% block table %}
    <table class="table table-striped table-bordered bootstrap-datatable">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ price.nom }}</td>
            </tr>
            <tr>
                <th>Starter</th>
                <td>{{ price.starter }} XAF</td>
                 <td>{{ price.starterDelay }} jrs</td>
                 <td>{{ price.starterDesc }}</td>
            </tr>
            <tr>
                <th>Standard</th>
                <td>{{ price.standard }} XAF</td>
                <td>{{ price.standardDelay }} jrs</td>
                <td>{{ price.standardDesc }}</td>
            </tr>
            <tr>
                <th>Premium</th>
                <td>{{ price.premium }} XAF</td>
                 <td>{{ price.premiumDelay }} jrs</td>
                 <td>{{ price.premiumDesc }}</td>
            </tr>                     
        </tbody>
    </table>
    <table class="table  no-table-bordered bootstrap-datatable">
        <tr> 
         <td>
            <a href="{{ path('price_index') }}">Retour</a>
        </td>
       
        <td>
            <a href="{{ path('price_edit', { 'id': price.id }) }}">Modifier</a>
        </td>
         <td>
           {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </td>
          </tr> 
    </table>

{% endblock %}
